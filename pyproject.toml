[project]
name = "dbt-core-snowflake"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sai_Teja_Psk", email = "34715617+satyakrishnapst@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.12.0",
    "dbt-snowflake>=1.12.0",
]

[project.scripts]
dbt-core-snowflake = "dbt_core_snowflake:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
